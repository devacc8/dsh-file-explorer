node_modules/
*.log
.DS_Store
TUI-Assessment.md

# Local pnpm tooling (registry mirror is user-specific)
.npmrc
pnpm-lock.yaml
pnpm-workspace.yaml

# Local type-only install used to run tsc when building lib/ (never commit it).
node_modules-types/
